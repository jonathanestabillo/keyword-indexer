// 'use strict';
// const indexIpcMain = require('electron').ipcMain;

// indexIpcMain.on('req-parse-indexinfo', (event, arg) => {
//   event.sender.send('res-parse-indexinfo', parser.exec(arg, db));
// });