// 'use strict';
// const indexIpcMain = require('electron').ipcMain;

// indexIpcMain.on('req-parse-indexlist', (event, arg) => {
//   event.sender.send('res-parse-indexlist', parser.exec(arg, db));
// });